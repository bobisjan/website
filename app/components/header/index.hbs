<header local-class="masthead">
  <h3 local-class="masthead-brand">
    <Gravatar
      @hash={{@gravatar}}
      @size={{50}}
      @retina={{true}}
      alt="avatar"
    />
  </h3>

  <nav local-class="masthead-nav">
    {{#each @links as |link|}}
      <a href={{link.url}} local-class="nav-link" target="_blank" rel="noopener">
        {{link.title}}
      </a>
    {{/each}}
  </nav>
</header>